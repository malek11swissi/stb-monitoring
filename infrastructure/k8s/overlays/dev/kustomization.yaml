apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: stb-dev
resources: [../../base, namespace.yaml]
patches:
  - target: { kind: Deployment }
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
