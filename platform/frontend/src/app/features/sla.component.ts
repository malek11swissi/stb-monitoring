/** Administration des délais SLA associés aux priorités d'incident. */
import {CommonModule} from '@angular/common';
import {Component,OnInit} from '@angular/core';
import {FormsModule} from '@angular/forms';
import {OperationsService,SlaPolicy} from '../core/operations.service';

@Component({standalone:true,imports:[CommonModule,FormsModule],styleUrls:['./sla.design.css'],template:`
<section class="page sla-page"><header class="sla-hero"><div><span class="eyebrow">MANAGER IT · GOUVERNANCE SLA</span><h1>Politiques de service</h1><p>Des délais clairs pour prioriser chaque incident et anticiper les dépassements.</p></div><button class="primary create-action" type="button" (click)="create()"><span>＋</span> Nouvelle politique</button></header>
<div class="sla-overview"><article><span class="overview-icon all">◷</span><div><small>POLITIQUES</small><b>{{policies.length}}</b></div></article><article><span class="overview-icon enabled">✓</span><div><small>ACTIVES</small><b>{{activeCount}}</b></div></article><article><span class="overview-icon paused">Ⅱ</span><div><small>INACTIVES</small><b>{{policies.length-activeCount}}</b></div></article></div>
@if(error){<div class="error">{{error}}</div>}@if(message){<div class="success">{{message}}</div>}
<div class="sla-grid">@for(item of policies;track item.id){<article class="card policy-card" [class.inactive]="!item.isActive"><div class="card-head"><span [class]="'priority '+item.priority.toLowerCase()">{{priorityLabel(item.priority)}}</span><span [class]="item.isActive?'status active':'status inactive'">{{item.isActive?'● Active':'○ Inactive'}}</span></div><h2>{{item.name}}</h2><p>{{item.description||'Aucune description'}}</p><div class="metrics"><div><span class="metric-glyph">◷</span><b>{{duration(item.responseTimeMinutes)}}</b><small>Prise en charge</small></div><div><span class="metric-glyph">✓</span><b>{{duration(item.resolutionTimeMinutes)}}</b><small>Résolution</small></div><div><span class="metric-glyph">△</span><b>{{item.warningPercentage}} %</b><small>Seuil d’alerte</small></div></div><div class="actions policy-actions"><button class="policy-action edit-action" type="button" (click)="edit(item)" [attr.aria-label]="'Modifier '+item.name"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 20h9M16.5 3.5a2.12 2.12 0 0 1 3 3L9 17l-4 1 1-4L16.5 3.5Z"/></svg>Modifier</button><button class="policy-action" [class.pause-action]="item.isActive" [class.resume-action]="!item.isActive" type="button" (click)="toggle(item)" [attr.aria-label]="(item.isActive?'Désactiver':'Activer')+' '+item.name">@if(item.isActive){<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="4" y="3" width="16" height="18" rx="4"/><path d="M10 9v6m4-6v6"/></svg>}@else{<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="m10 8 6 4-6 4V8Z"/></svg>}{{item.isActive?'Désactiver':'Activer'}}</button></div></article>}@empty{<div class="card empty"><span>◷</span><h2>Aucune politique SLA</h2><p>Créez une politique pour définir les délais de prise en charge et de résolution.</p><button class="primary" type="button" (click)="create()">＋ Créer une politique</button></div>}</div>
@if(draft){<div class="modal-backdrop" (click)="draft=null"><form #form="ngForm" class="card modal form sla-modal" (click)="$event.stopPropagation()" (ngSubmit)="save()"><div class="sla-modal-head"><span class="modal-icon">◷</span><div><span class="eyebrow">CONFIGURATION SLA</span><h2>{{editing?'Modifier':'Créer'}} une politique</h2><p>Définissez des objectifs mesurables pour cette priorité.</p></div><button class="close-modal" type="button" (click)="draft=null" aria-label="Fermer">×</button></div><label>Nom<input name="name" [(ngModel)]="draft.name" required minlength="3" placeholder="Ex. Incident critique"></label><label>Description<textarea name="description" [(ngModel)]="draft.description" rows="3" placeholder="À quel type d’intervention cette politique s’applique-t-elle ?"></textarea></label><label>Priorité<select name="priority" [(ngModel)]="draft.priority"><option value="P1Critical">P1 · Critique</option><option value="P2High">P2 · Haute</option><option value="P3Medium">P3 · Moyenne</option><option value="P4Low">P4 · Faible</option></select></label><div class="two"><label>Prise en charge (min)<input type="number" name="response" [(ngModel)]="draft.responseTimeMinutes" min="1" required></label><label>Résolution (min)<input type="number" name="resolution" [(ngModel)]="draft.resolutionTimeMinutes" min="1" required></label></div><label>Seuil d’avertissement (%)<input type="number" name="warning" [(ngModel)]="draft.warningPercentage" min="1" max="90" required><small>Une alerte apparaît lorsque ce pourcentage du délai est consommé.</small></label><div class="actions end modal-actions"><button type="button" (click)="draft=null">Annuler</button><button class="primary" [disabled]="form.invalid||saving">{{saving?'Enregistrement…':'✓ Enregistrer la politique'}}</button></div></form></div>}</section>`,styles:[`.sla-page{max-width:1200px}.sla-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(270px,1fr));gap:16px}.sla-grid .card{border-top:4px solid #0b8a80}.sla-grid .inactive{opacity:.65;border-top-color:#94a3b8}.card-head,.actions,.two{display:flex;gap:10px;justify-content:space-between}.priority,.status{padding:5px 9px;border-radius:20px;font-size:11px;font-weight:800}.priority.p1critical{background:#fee2e2;color:#b91c1c}.priority.p2high{background:#ffedd5;color:#c2410c}.priority.p3medium{background:#fef3c7;color:#a16207}.priority.p4low{background:#dbeafe;color:#1d4ed8}.status.active{background:#dcfce7;color:#15803d}.status.inactive{background:#e2e8f0;color:#475569}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:20px 0}.metrics div{background:#f4f7f9;border-radius:10px;padding:12px}.metrics b,.metrics small{display:block}.metrics small{color:#718294;margin-top:5px}.actions button{padding:8px 12px}.modal{width:min(590px,95vw)}.form{display:grid;gap:13px}.form label{display:grid;gap:6px}.form input,.form textarea,.form select{padding:10px;border:1px solid #cad7df;border-radius:8px}.two label{flex:1}.end{justify-content:flex-end}@media(max-width:550px){.two{display:grid}.metrics{grid-template-columns:1fr}}`]})
export class SlaComponent implements OnInit{policies:SlaPolicy[]=[];draft:any=null;editing:SlaPolicy|null=null;error='';message='';saving=false;constructor(private api:OperationsService){}ngOnInit(){this.load()}get activeCount(){return this.policies.filter(x=>x.isActive).length}priorityLabel(value:string){return({P1Critical:'P1 · Critique',P2High:'P2 · Haute',P3Medium:'P3 · Moyenne',P4Low:'P4 · Faible'} as Record<string,string>)[value]||value}load(){this.api.slas().subscribe({next:x=>this.policies=x,error:e=>this.fail(e)})}create(){this.editing=null;this.draft={name:'',description:'',priority:'P3Medium',responseTimeMinutes:30,resolutionTimeMinutes:240,warningPercentage:75}}edit(x:SlaPolicy){this.editing=x;this.draft={...x}}save(){this.saving=true;this.api.saveSla(this.draft,this.editing?.id).subscribe({next:()=>{this.saving=false;this.draft=null;this.message='Politique SLA enregistrée.';this.load()},error:e=>{this.saving=false;this.fail(e)}})}toggle(x:SlaPolicy){this.api.slaActive(x.id,!x.isActive).subscribe({next:()=>this.load(),error:e=>this.fail(e)})}duration(minutes:number){return minutes<60?`${minutes} min`:minutes%60?`${Math.floor(minutes/60)} h ${minutes%60} min`:`${minutes/60} h`}fail(e:any){this.error=e.error?.message||`Erreur HTTP ${e.status||0}`}}
