/** Journal d'audit avec filtres combinables pour les investigations administrateur. */
import{Component,OnInit}from'@angular/core';import{FormsModule}from'@angular/forms';import{DatePipe}from'@angular/common';import{HttpClient}from'@angular/common/http';
interface AuditLog{id:string;userId?:string;action:string;entityName:string;entityId?:string;detail?:string;ipAddress?:string;createdAt:string;success:boolean}
@Component({standalone:true,imports:[FormsModule,DatePipe],template:`<section class="page audit"><header><span>TRAÇABILITÉ & SÉCURITÉ</span><h1>Journal d’audit</h1><p>Recherchez rapidement une connexion ou une action sensible.</p></header><section class="filters card"><label class="search">⌕<input [(ngModel)]="query" placeholder="Action, identifiant, IP..."></label><select [(ngModel)]="entity"><option value="">Toutes les entités</option>@for(x of entities;track x){<option>{{x}}</option>}</select><select [(ngModel)]="result"><option value="">Tous les résultats</option><option value="success">Succès</option><option value="failure">Échec</option></select><input type="date" [(ngModel)]="date">@if(hasFilters){<button (click)="reset()">↻ Réinitialiser</button>}</section><div class="result-line"><b>{{filtered.length}} événement{{filtered.length>1?'s':''}}</b><span>sur {{logs.length}}</span></div><div class="card table-wrap"><table><thead><tr><th>Date et heure</th><th>Action</th><th>Cible</th><th>Utilisateur</th><th>Adresse IP</th><th>Résultat</th></tr></thead><tbody>@for(log of filtered;track log.id){<tr><td>{{log.createdAt|date:'dd/MM/yyyy HH:mm:ss'}}</td><td><strong>{{label(log.action)}}</strong><br><small>{{log.detail}}</small></td><td>{{log.entityName}}<br><small>{{log.entityId}}</small></td><td><small>{{log.userId||'Anonyme'}}</small></td><td>{{log.ipAddress||'—'}}</td><td><span [class]="log.success?'status active':'status inactive'">{{log.success?'✓ Succès':'✕ Échec'}}</span></td></tr>}@empty{<tr><td colspan="6" class="empty">Aucun événement ne correspond aux filtres.</td></tr>}</tbody></table></div></section>`,styles:[`.audit{max-width:1350px}.audit header>span{font-size:10px;font-weight:900;letter-spacing:.16em;color:#087e8b}.audit h1{font-size:36px;margin:6px 0;color:#123b5d}.audit header p{color:#61727e}.filters{display:flex;align-items:center;gap:9px;padding:13px;margin-top:20px}.search{display:flex!important;flex:1;align-items:center;gap:8px;border:1px solid #bdcdd5;border-radius:9px;padding:0 11px}.search input{border:0;outline:0;width:100%}.filters select,.filters>input{min-width:170px}.filters button{padding:11px;border:1px solid #d4e0e5;border-radius:9px;background:#fff}.result-line{display:flex;justify-content:space-between;padding:15px 3px;color:#61727e}.table-wrap{padding:0;border-radius:15px}th{background:#f6f9fa;font-size:11px;text-transform:uppercase;letter-spacing:.04em}td{font-size:12px}.empty{text-align:center;padding:35px}@media(max-width:850px){.filters{flex-wrap:wrap}.search{min-width:100%}}`]})
export class AuditComponent implements OnInit{logs:AuditLog[]=[];query='';entity='';result='';date='';constructor(private http:HttpClient){}get entities(){return[...new Set(this.logs.map(x=>x.entityName))].sort()}get hasFilters(){return!!(this.query||this.entity||this.result||this.date)}get filtered(){const q=this.query.toLowerCase().trim();return this.logs.filter(x=>(!q||[x.action,x.entityName,x.entityId,x.userId,x.ipAddress,x.detail].some(v=>v?.toLowerCase().includes(q)))&&(!this.entity||x.entityName===this.entity)&&(!this.result||(this.result==='success')===x.success)&&(!this.date||x.createdAt.slice(0,10)===this.date))}ngOnInit(){this.http.get<AuditLog[]>('http://localhost:5041/api/audit').subscribe(x=>this.logs=x)}reset(){this.query=this.entity=this.result=this.date=''}label(x:string){return x.replaceAll('_',' ')}}
