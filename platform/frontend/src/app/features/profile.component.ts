import {Component} from '@angular/core';import {FormsModule} from '@angular/forms';import {HttpClient} from '@angular/common/http';import {AuthService,User} from '../core/auth.service';
@Component({standalone:true,imports:[FormsModule],template:`<section class="page"><h1>Mon profil</h1><div class="grid"><form #profileForm="ngForm" class="card form" (ngSubmit)="save()"><h2>Informations</h2><label>Prénom<input #first="ngModel" name="firstName" [(ngModel)]="firstName" required>@if(first.touched&&first.invalid){<small class="field-error">Prénom obligatoire.</small>}</label><label>Nom<input #last="ngModel" name="lastName" [(ngModel)]="lastName" required>@if(last.touched&&last.invalid){<small class="field-error">Nom obligatoire.</small>}</label><label>E-mail<input #mail="ngModel" name="email" [(ngModel)]="email" type="email" required email>@if(mail.touched&&mail.invalid){<small class="field-error">Adresse e-mail invalide.</small>}</label><button class="primary" [disabled]="profileForm.invalid">Enregistrer</button></form><form #passwordForm="ngForm" class="card form" (ngSubmit)="changePassword()"><h2>Mot de passe</h2><label>Mot de passe actuel<input #current="ngModel" name="current" [(ngModel)]="currentPassword" type="password" required>@if(current.touched&&current.invalid){<small class="field-error">Mot de passe actuel obligatoire.</small>}</label><label>Nouveau mot de passe<input #next="ngModel" name="next" [(ngModel)]="newPassword" type="password" minlength="8" required>@if(next.touched&&next.invalid){<small class="field-error">8 caractères minimum.</small>}</label><button class="primary" [disabled]="passwordForm.invalid">Modifier</button></form></div>@if(message){<p class="success">{{message}}</p>}@if(error){<p class="error">{{error}}</p>}</section>`})
export class ProfileComponent{firstName='';lastName='';email='';currentPassword='';newPassword='';message='';error='';readonly api='http://localhost:5041/api/auth';constructor(private http:HttpClient,private auth:AuthService){const u=auth.currentUser();if(u){this.firstName=u.firstName;this.lastName=u.lastName;this.email=u.email}}save(){this.reset();this.http.put<User>(`${this.api}/profile`,{firstName:this.firstName,lastName:this.lastName,email:this.email}).subscribe({next:u=>{localStorage.setItem('stb_user',JSON.stringify(u));this.auth.currentUser.set(u);this.message='Profil mis à jour.'},error:e=>this.error=e.error?.message||'Modification impossible.'})}changePassword(){this.reset();this.http.post(`${this.api}/change-password`,{currentPassword:this.currentPassword,newPassword:this.newPassword}).subscribe({next:()=>{this.currentPassword='';this.newPassword='';this.message='Mot de passe modifié.'},error:e=>this.error=e.error?.message||'Modification impossible.'})}reset(){this.message='';this.error=''}}
