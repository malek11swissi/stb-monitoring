import { Component } from '@angular/core';import { FormsModule } from '@angular/forms';import { HttpClient } from '@angular/common/http';import { AuthService,User } from '../core/auth.service';
@Component({standalone:true,imports:[FormsModule],template:`<section class="page"><h1>Mon profil</h1><div class="card"><form class="form" (ngSubmit)="save()"><label>Prénom<input name="firstName" [(ngModel)]="firstName" required></label><label>Nom<input name="lastName" [(ngModel)]="lastName" required></label><label>E-mail<input name="email" [(ngModel)]="email" type="email" required></label>@if(message){<div class="badge">{{message}}</div>}@if(error){<div class="error">{{error}}</div>}<button class="primary">Enregistrer</button></form></div></section>`})
export class ProfileComponent{firstName='';lastName='';email='';message='';error='';constructor(private http:HttpClient,private auth:AuthService){const u=auth.currentUser();if(u){this.firstName=u.firstName;this.lastName=u.lastName;this.email=u.email;}}save(){this.message='';this.error='';this.http.put<User>('http://localhost:5041/api/auth/profile',{firstName:this.firstName,lastName:this.lastName,email:this.email}).subscribe({next:u=>{localStorage.setItem('stb_user',JSON.stringify(u));this.auth.currentUser.set(u);this.message='Profil mis à jour.';},error:e=>this.error=e.error?.message||'Modification impossible.'});}}
